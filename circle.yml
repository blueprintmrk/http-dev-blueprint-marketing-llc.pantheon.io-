general:
  artifacts:
    - "pkg"

machine:
  services:
    - docker

dependencies:
  pre:
    - make docker-deps

test:
  override:
    - make all

deployment:
  dev:
    branch:
      - stage
      - yolo
      - dev
    commands:
      - make stage_deploy
  prod:
    branch: master
    commands:
      - make prod_deploy
